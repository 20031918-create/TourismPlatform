@{
    ViewBag.Title = "Destination Details";
}

<div class="container mt-4">
    <h2>@ViewBag.Name</h2>
    <p>Welcome to @ViewBag.Name.</p>

    <a class="btn btn-secondary" href="@Url.Action("Index","Destinations")">
        Back
    </a>
</div>
